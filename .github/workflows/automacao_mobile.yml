name: Testes Mobile Locais com Appium

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  android-tests:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🐳 Build da imagem Docker
        run: docker build -t mobile-tests .

      - name: ▶️ Executar os testes no container
        run: docker run --rm mobile-tests
