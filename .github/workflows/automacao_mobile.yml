name: Mobile Tests (Working Version)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐳 Build and run tests
        run: |
          docker build -t mobile-tests . && \
          docker run --rm \
            --device /dev/kvm \
            -v $(pwd)/results:/app/results \
            -v $(pwd)/apps:/app/apps \
            mobile-tests

      - name: 📤 Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: results/